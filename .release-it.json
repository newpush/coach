{
  "hooks": {
    "after:bump": "pnpm run cli changelog --write --version ${version} --from ${latestTag} && git add USER_CHANGELOG.md public/content/releases/",
    "after:release": "pnpm run cli changelog publish && pnpm run cli changelog publish --file CHANGELOG.md --type technical"
  },
  "plugins": {
    "@release-it/conventional-changelog": {
      "preset": "angular",
      "infile": "CHANGELOG.md"
    }
  },
  "npm": {
    "publish": false
  },
  "git": {
    "commitMessage": "chore: release ${version}",
    "tagName": "v${version}"
  },
  "github": {
    "release": true,
    "releaseName": "v${version}",
    "tokenRef": "GITHUB_TOKEN"
  }
}
