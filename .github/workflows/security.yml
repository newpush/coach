name: Security Analysis

on:
  push:
    branches: ['master', 'develop']
  pull_request:
    branches: ['develop']
    paths-ignore:
      - 'docs/**'
      - '**.md'
      - '.gitignore'
      - 'LICENSE'
  schedule:
    - cron: '0 4 * * 1'

permissions:
  contents: read
  security-events: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  analyze:
    name: CodeQL Security Scan
    runs-on: [self-hosted, shadow]
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        language: ['javascript-typescript']

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Initialize CodeQL
        uses: github/codeql-action/init@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4
        with:
          languages: ${{ matrix.language }}
          queries: security-extended
          # The shared host has 62 GB RAM / 32 CPUs, but each runner is capped
          # at 14 GB. Keep CodeQL below the runner cgroup instead of allowing
          # host-level auto-detection to request ~60 GB and 32 threads.
          ram: 8192
          threads: 4

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@f205ea1c3313d32999d8d6a48b4f6530d4437b38 # v4
        with:
          category: '/language:${{matrix.language}}'
